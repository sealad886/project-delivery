# Artifact templates

The installable canonical document is [`skills/.shared/artifact-templates.md`](../skills/.shared/artifact-templates.md). It lives inside the manifest-declared `skills/` tree so marketplace mirrors retain every runtime dependency without exposing another skill.
