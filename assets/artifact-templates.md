# Artifact templates

The installable canonical document is [`skills/.shared/artifact-templates.md`](../plugins/project-delivery/skills/.shared/artifact-templates.md). It lives inside the manifest-declared `skills/` tree so the standalone plugin retains every runtime dependency without exposing another skill.
