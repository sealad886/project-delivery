# External systems contract

The installable canonical document is [`skills/.shared/external-systems.md`](../skills/.shared/external-systems.md). It lives inside the manifest-declared `skills/` tree so the standalone plugin retains every runtime dependency without exposing another skill.
