# Shared operating model

The installable canonical document is [`skills/.shared/operating-model.md`](../plugins/project-delivery/skills/.shared/operating-model.md). It lives inside the manifest-declared `skills/` tree so the standalone plugin retains every runtime dependency without exposing another skill.
