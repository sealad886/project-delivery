# Shared operating model

The installable canonical document is [`skills/.shared/operating-model.md`](../skills/.shared/operating-model.md). It lives inside the manifest-declared `skills/` tree so marketplace mirrors retain every runtime dependency without exposing another skill.
